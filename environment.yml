name: NCA-Trak
channels:
  - defaults
dependencies:
  - faker
  - pandas
  - python=3.10
  - rich
  - numpy=1.24
  - pip
  - pip:
      - tkcalendar
      - sv-ttk
      - faker_education
prefix: /home/zanel/anaconda3/envs/NCA-Trak
